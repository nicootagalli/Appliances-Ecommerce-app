server:
  port: 8081

spring:
  application:
    name: config-server
  profiles:
    active: native

  cloud:
    config:
      server:
        git:
          uri: https://github.com/nicootagalli/Appliances-Ecommerce-app/tree/api_gateway_test
          search-paths: config-data

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false