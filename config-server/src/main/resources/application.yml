server:
  port: 8081

spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/nicootagalli/Appliances-Ecommerce-app
          search-paths: config-data

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false