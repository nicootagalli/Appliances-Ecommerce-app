spring:
  application:
    name: cart-service

  datasource:
    url: jdbc:mysql://localhost:3306/service-cart?useSSL=false&serverTimezone=UTC
    username: root
    password: 140204150499

  jpa:
    hibernate:
      ddl-auto: update

server:
  port: 8085

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka